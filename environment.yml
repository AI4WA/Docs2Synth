name: Docs2Synth
channels:
  - defaults
  - conda-forge
dependencies:
  - pip
  - python=3.10
  - pytest
  - black
  - isort
  - flake8
  - pyyaml
  - pandas
  - pillow
  # Note: PyTorch, PaddlePaddle, and other dependencies will be installed via pip
  # using requirements.txt files to properly handle CPU/GPU variants
